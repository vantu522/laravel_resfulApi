[phases.setup]
nixPkgs = ['php82', 'php82Packages.composer']

[phases.build]
cmds = ['composer install', 'php artisan key:generate', 'php artisan migrate --force']

[start]
cmd = 'php artisan serve --host=0.0.0.0 --port=$PORT'